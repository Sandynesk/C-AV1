#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>

/**
 * @brief Lê uma string de forma segura do stdin.
 * (Esta é uma função utilitária que será usada depois)
 */
int read_safe_string(char *buffer, int size);

#endif // UTILS_H